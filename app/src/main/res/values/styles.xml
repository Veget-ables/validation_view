<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyValidationTheme" parent="Widget.Design.TextInputLayout">
        <item name="errorTextAppearance">@style/MyValidationTheme.Error</item>
    </style>

    <style name="MyValidationTheme.Error" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textColor">@color/colorAccent</item>
    </style>

</resources>
